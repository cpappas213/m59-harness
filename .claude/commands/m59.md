---
description: Meridian 59 fleet board — who is up, who is behind a wall, who is stuck
argument-hint: [fleet]
---

The fleet as it is right now. Runs against the live broker on 8901.

**Which fleet this is.** The broker holds exactly one, so the board is whatever it is
holding — not whatever this checkout defaults to. When those two disagree the line below
says so, and the board that follows is the broker's answer.

!`node tools/m59-which.mjs --fleet $ARGUMENTS 2>&1`

!`node tools/m59-status.mjs --once 2>/dev/null || node tools/m59-status.mjs`

## Board

!`node -e "
const P=process.env.M59_BROKER_PORT||'8901';
const call=async(n,a={},ms=15000)=>{const c=new AbortController();const t=setTimeout(()=>c.abort(),ms);
 try{const r=await fetch('http://127.0.0.1:'+P+'/',{method:'POST',headers:{'content-type':'application/json'},signal:c.signal,
 body:JSON.stringify({jsonrpc:'2.0',id:1,method:'tools/call',params:{name:n,arguments:a}})});
 const j=await r.json();return JSON.parse(j.result.content[0].text);}catch(e){return null}finally{clearTimeout(t)}};
const l=await call('autopilot',{agent:'any',action:'list'},8000);
if(!l){console.log('broker not answering on '+P);process.exit(0)}
const f=await call('fleet',{});
const by=new Map((f?.fleet||[]).map(r=>[r.agent,r]));
console.log('char        level  health   vigor  wpn food  spot   doing');
for(const a of l.autopilots){const r=by.get(a.name)||{};
 const spot=a.safe_spot?(a.safe_spot.works?'WALL':'test'):'-';
 console.log(String(r.character||a.name).padEnd(11),String(r.level??'?').padStart(5),
  String(r.health??'?').padStart(8),String((r.vigor_of||'?').split('/')[0]).padStart(6),
  (r.has_weapon?'  y':'  N').padStart(5),(r.has_food?'y':'N').padStart(5),
  spot.padStart(6),' ',String(a.activity||'').slice(0,38));}
const st=l.autopilots.filter(a=>a.stalled);
if(st.length)console.log('\nSTALLED: '+st.map(a=>a.name+' ('+a.stalled.why+')').join(', '));
" 2>&1`

Read the board above and tell me, briefly — naming the fleet it is about:
- anything **stalled**, and what it is waiting for
- anyone with **no weapon** or **no food** (both fail silently — an unarmed character
  punches monsters, and one with no food never gets vigor past the resting cap of 80)
- anyone whose **vigor** is low enough that they cannot farm regardless of health
- whether any character has died in a square it believed was a safe spot

Do not change anything unless I ask.
